import { getZodRule, getZodValidationObject } from './useGetZodValidator';

describe('getZodValidationObject', () => {
    it('fail', () => {
        expect(false).toBe(true);
    });
});

describe('getZodRule', () => {
    it('fail', () => {
        expect(false).toBe(true);
    });
});
