import { Locale } from 'pydantic-forms';

/**
 * Shared constants for all example forms
 */

export const DEFAULT_LOCALE = Locale.enGB;

export const DEFAULT_TRANSLATIONS = {
    renderForm: { loading: 'The form is loading. Please wait.' },
};
