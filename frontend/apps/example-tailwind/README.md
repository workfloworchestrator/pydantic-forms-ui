Notes:

created -> 
postcss.config.js

Not sure if that is correct location. src/app Could be better?

Also not sure if an extra app in the mono repo is desired; we could also serve examples from one webserver which limit boilerplate
